app:
  name: nensyuapuri
  home: c:\${app.name}

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:c:\${app.name}\db\${app.name}
    username: sa
    password: 
    
server:
 port: 8085